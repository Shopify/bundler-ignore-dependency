name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  versions:
    runs-on: ubuntu-latest
    outputs:
      bundler-versions: ${{ steps.versions.outputs.bundler-versions }}
    steps:
      - name: Fetch latest Bundler versions
        id: versions
        run: |
          versions=$(curl -s https://rubygems.org/info/bundler | \
            grep "^[0-9]" | sed 's/ .*//' | sort -V | \
            awk -F. '!seen[$1]++ {print $1}' | tail -2 | \
            while read major; do
              curl -s https://rubygems.org/info/bundler | \
                grep "^$major\." | sed 's/ .*//' | sort -V | tail -1
            done | jq -R . | jq -s .)
          echo "bundler-versions=$versions" >> $GITHUB_OUTPUT

  test:
    runs-on: ubuntu-latest
    needs: versions

    strategy:
      fail-fast: false
      matrix:
        ruby-version:
          - "3.3"
          - "3.4"
          - "4.0"
        bundler-version: ${{ fromJson(needs.versions.outputs.bundler-versions) }}

    steps:
      - uses: actions/checkout@v4

      - name: Set up Ruby ${{ matrix.ruby-version }} with Bundler ${{ matrix.bundler-version }}
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: ${{ matrix.ruby-version }}
          bundler: ${{ matrix.bundler-version }}
          bundler-cache: true

      - name: Install dependencies
        run: bundle install

      - name: Lint with RuboCop
        run: bin/rubocop

      - name: Run tests
        run: bin/minitest
